# Stickies and presence (optional). Create a Supabase project, run
# supabase/schema.sql in its SQL editor, then copy these from
# Project Settings → API. Without them, `astro dev` uses a local stand-in
# and production builds hide the features.
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=
